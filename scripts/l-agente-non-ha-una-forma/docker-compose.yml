services:
  redis:
    image: redis:8-alpine
    container_name: agente-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --save 60 1 --appendonly yes

volumes:
  redis-data:
