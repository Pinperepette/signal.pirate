services:
  redis:
    image: redis:8-alpine
    container_name: agd-lab-redis
    ports:
      - "127.0.0.1:6391:6379"
    command: redis-server --appendonly no --save ""
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 1s
      retries: 5
